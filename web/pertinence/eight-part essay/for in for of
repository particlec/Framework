for...of遍历获取的是对象的键值, for...in获取的是对象的键名;
for...in会遍历对象的整个原型链, 性能非常差不推荐使用,而for...of只遍历当前对象不会遍历原型链;
对于数组的遍历,for...in会返回数组中所有可枚举的属性(包括原型链上可枚举的属性),for...of只返回数组的下标对应的属性值;
总结：for...in循环主要是为了遍历对象而生,不适用遍历数组; for....of循环可以用来遍历数组、类数组对象、字符串、Set、Map以及Generator对象


Web存储是前端开发中的一项关键技术，包括localStorage和sessionStorage。它们提供了在浏览器中存储数据的能力，
可以在页面刷新或关闭后仍然保留数据。localStorage存储的数据在不同浏览器窗口或标签页之间共享，
而sessionStorage存储的数据只在当前会话中有效。
通过使用这两种存储方式，可以在前端实现数据的持久化存储和临时存储，方便实现用户偏好设置、缓存数据等功能，提升用户体验