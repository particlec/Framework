跨域相关

基本理解
1、是浏览器的同源策略导致的，当一个请求的url的协议、域名、端口三者之间任意一个与当前页面url不同即为跨域

跨域解决方法：

实际上：本地调试的时候，写一个Node中间件代理 ，// 配置 cors
中间服务器关闭了跨域功能 app.use(cors())

1、Nginx反向代理

最简单的跨域的方式，实现原理类似与Node中间件代理，需要搭建一个中转Nginx服务器，用于转发请求。
只需配置nginx，并在相应文件中配置端口号