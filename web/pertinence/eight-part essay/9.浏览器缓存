浏览器缓存
浏览器缓存包含 ： HTTP 缓存、indexDB、cookie、localstorage 、sessionStorage
https://blog.csdn.net/jiang_ziY/article/details/123818243？

浏览器缓存包含 ： HTTP 缓存、indexDB、cookie、localstorage 、sessionStorage


HTTP 缓存 ：有一个缓存控制的属性 ：Cache-control

Cookie ：它的大小限制为4KB以内。可以缓存用户登录信息（例如：token）
每次都会携带在HTTP头中,在浏览器和服务器之间传递

localStorage：就四个方法：localStorage.setItem('属性名',属性值)
localStorage.getItem('属性名')、localStorage.removeItem('属性名')、localStorage.clear()

属性值 string  ，就算是传了 true 也是 string

 sessionStorage : 关闭界面就会 没，方法与上面一样